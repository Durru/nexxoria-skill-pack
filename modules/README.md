# Deprecated Root Modules

These root-level `modules/` files are legacy from the pre-SDK architecture.

## Active source of truth

The active Nexxoria implementation now lives in:

- `packages/sdk/src/modules/`

Do not add new active product logic here.

If a change belongs to Nexxoria system behavior, implement it in the SDK.
