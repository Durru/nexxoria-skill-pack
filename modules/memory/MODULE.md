# Memory Module

Memory persists project knowledge across the system in global, stage, and tag scopes.

Memory decides what should be remembered and in which scope. Runtime only writes the resulting artifacts.

Executable entrypoint: `modules/memory/index.js`
